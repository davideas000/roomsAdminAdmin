import { RaOverlayPanelTriggerDirective } from './overlay-panel-trigger.directive';

describe('RaOverlayPanelTriggerDirective', () => {
  it('should create an instance', () => {
    // const directive = new RaOverlayPanelTriggerDirective();
    // expect(directive).toBeTruthy();
  });
});
